# 实施阶段 v0.1

## Phase 1：审计与数据汇总

执行：

```text
SIM-C001
SIM-C002
```

验收：

```text
所有 registry 可加载
所有 JSON 校验通过
旧数据和新数据关系明确
```

## Phase 2：命盘与天命升级

执行：

```text
SIM-C003
```

验收：

```text
九宫评分可用
天命成立条件可用
不合理天命可变异
创建页可显示真实命盘
```

## Phase 3：身世与主线升级

执行：

```text
SIM-C004
SIM-C005
```

验收：

```text
身世/随身物生命周期可推进
人生主线可评分
事件线可推进
玩法插曲候选可生成
阶段身份可变化
```

## Phase 4：月度与半年选择升级

执行：

```text
SIM-C006
SIM-C007
```

验收：

```text
216 个月可推进
每 6 个月可生成选择
事件受命盘和主线影响
隐藏分支不泄露真名
```

## Phase 5：人生模拟 UI

执行：

```text
SIM-C008
```

验收：

```text
玩家能完整体验 0–18 岁模拟
支持速度控制、暂停、继续、日志、阶段总结
```

## Phase 6：叙事增强

执行：

```text
SIM-C009
```

验收：

```text
LLM 关闭时可运行
Fallback 模板可用
安全脱敏通过
```

## Phase 7：成年节点和首个试炼桥接

执行：

```text
SIM-C010
```

验收：

```text
成年节点可生成候选路径
默认 fallback 可进入首个试炼或洞府开启
不重复结算
```

## Phase 8：E2E 与 RC

执行：

```text
SIM-C011
```

验收：

```text
新游戏完整链路跑通
关键截图齐全
Blocker 清单明确
```
